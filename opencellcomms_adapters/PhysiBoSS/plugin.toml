# OpenCellComms plugin manifest. See docs/PLUGINS.md for the full schema.
[plugin]
name = "PhysiBoSS"
version = "0.1.0"
description = "PhysiCell / PhysiBoSS code-generation nodes (substrate, cell types, Hill rules, project templates, simulation run)."
author = ""
engine_version = ">=0.0.0"
compatible_kernels = ["biophysics", "physicell"]
