"""OpenCellComms evaluation harness.

Measures whether the constrained authoring environment (workflow IR, role-aware
scaffolds, canonical examples, static validators, the /occ_* protocols) lets a
coding agent turn a MODEL.md into correctly-structured, runnable ABM code in
fewer correction rounds. See evals/README.md.
"""
