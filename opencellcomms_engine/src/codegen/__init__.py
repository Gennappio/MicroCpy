"""Code generation for external simulation engines.

OpenCellComms generates runnable project trees (XML config, custom_modules,
Makefile, etc.) so it can drive third-party engines as black boxes without
forking them. See ``docs/Physicell_Facade_plan.md``.
"""
